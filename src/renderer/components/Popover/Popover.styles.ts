import { styled } from "@nextui-org/react";

export const Container = styled("div", {
  position: "absolute",
  width: "100%",
  background: "$accents1",
  marginTop: "$xs",
  zIndex: 1000,
  borderRadius: "$md",
});
